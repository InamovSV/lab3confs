app:
  service-name: client-service-2

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 1
    leaseExpirationDurationInSeconds: 2
    appname: client-service
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8010/eureka/
#    healthcheck:
#     enabled: true
server:
  port: 8002