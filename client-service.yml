app:
  service-name: client-service-3

eureka:
  instance:
    leaseRenewalIntervalInSeconds: 1
    leaseExpirationDurationInSeconds: 2
    appname: client-service
  client:
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://127.0.0.1:8010/eureka/
#    healthcheck:
#     enabled: true
server:
  port: 8002